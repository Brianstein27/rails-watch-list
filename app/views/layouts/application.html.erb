<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg mb-3 bg-body-tertiary">
      <div class="container-fluid">
        <%= link_to "BrianMDb", lists_path, class: "btn btn-secondary" %>
        <div class="navbar-nav">
          <%= link_to "Add list", new_list_path, class: "btn btn-primary" %>
        </div>
      </div>
    </nav>
    <div class="containter w-75 mx-auto">
      <%= yield %>
    </div>
  </body>
</html>
